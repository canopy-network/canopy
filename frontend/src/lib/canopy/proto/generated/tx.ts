// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: tx.proto

    /* eslint-disable */
import { Any } from './google/protobuf/any';
import { BinaryWriter, BinaryReader } from '@bufbuild/protobuf/wire';

export const protobufPackage = 'types';

/**
 * Transaction represents a request or action submitted to the network like transfer assets or perform other operations
 * within the blockchain system - THIS MUST MATCH lib.Transaction EXACTLY for signing
 */
export interface Transaction {
/** message_type: The type of the transaction like 'send' or 'stake' */
messageType: string,
/** msg: The actual transaction message payload, which is encapsulated in a generic message format */
msg: Any | undefined,
/** signature: The cryptographic signature used to verify the authenticity of the transaction */
signature: Signature | undefined,
/** created_height: The height when the transaction was created - allows 'safe pruning' */
createdHeight: bigint,
/** time: The timestamp when the transaction was created - used as temporal entropy to prevent hash collisions in txs */
time: bigint,
/** fee: The fee associated with processing the transaction */
fee: bigint,
/** memo: An optional message or note attached to the transaction */
memo: string,
/** network_id: The identity of the network the transaction is intended for */
networkId: bigint,
/** chain_id: The identity of the committee the transaction is intended for */
chainId: bigint,
/** nonce: The sender's protocol replay-protection nonce */
nonce: bigint,
}

/**
 * MessageSend is a standard transfer transaction, taking tokens from the sender and transferring
 * them to the recipient
 */
export interface MessageSend {
/** from_address: is the sender of the funds */
fromAddress: Uint8Array,
/** to_address: is the recipient of the funds */
toAddress: Uint8Array,
/** amount: is the amount of tokens in micro-denomination (uCNPY) */
amount: bigint,
}

/** FeeParams is the parameter space that defines various amounts for transaction fees */
export interface FeeParams {
/** send_fee: is the fee amount (in uCNPY) for Message Send */
sendFee: bigint,
}

/**
 * A Signature is a digital signature is a cryptographic "fingerprint" created with a private key,
 * allowing others to verify the authenticity and integrity of a message using the corresponding public key
 */
export interface Signature {
/** public_key: is a cryptographic code shared openly, used to verify digital signatures */
publicKey: Uint8Array,
/** signature: the bytes of the signature output from a private key which may be verified with the message and public */
signature: Uint8Array,
}

function createBaseTransaction(): Transaction {
      return { messageType: "",msg: undefined,signature: undefined,createdHeight: 0n,time: 0n,fee: 0n,memo: "",networkId: 0n,chainId: 0n,nonce: 0n };
    }

export const Transaction: MessageFns<Transaction> = {
              encode(
      message: Transaction,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.messageType !== "") {
          writer.uint32(10).string(message.messageType);
        }
if (message.msg !== undefined ) {
          Any.encode(message.msg, writer.uint32(18).fork()).join();
        }
if (message.signature !== undefined ) {
          Signature.encode(message.signature, writer.uint32(26).fork()).join();
        }
if ( message.createdHeight !== 0n) {
          if (BigInt.asUintN(64, message.createdHeight) !== message.createdHeight) {
          throw new globalThis.Error('value provided for field message.createdHeight of type uint64 too large');
        }
        writer.uint32(32).uint64(message.createdHeight);
        }
if ( message.time !== 0n) {
          if (BigInt.asUintN(64, message.time) !== message.time) {
          throw new globalThis.Error('value provided for field message.time of type uint64 too large');
        }
        writer.uint32(40).uint64(message.time);
        }
if ( message.fee !== 0n) {
          if (BigInt.asUintN(64, message.fee) !== message.fee) {
          throw new globalThis.Error('value provided for field message.fee of type uint64 too large');
        }
        writer.uint32(48).uint64(message.fee);
        }
if ( message.memo !== "") {
          writer.uint32(58).string(message.memo);
        }
if ( message.networkId !== 0n) {
          if (BigInt.asUintN(64, message.networkId) !== message.networkId) {
          throw new globalThis.Error('value provided for field message.networkId of type uint64 too large');
        }
        writer.uint32(64).uint64(message.networkId);
        }
if ( message.chainId !== 0n) {
          if (BigInt.asUintN(64, message.chainId) !== message.chainId) {
          throw new globalThis.Error('value provided for field message.chainId of type uint64 too large');
        }
        writer.uint32(72).uint64(message.chainId);
        }
if ( message.nonce !== 0n) {
          if (BigInt.asUintN(64, message.nonce) !== message.nonce) {
          throw new globalThis.Error('value provided for field message.nonce of type uint64 too large');
        }
        writer.uint32(80).uint64(message.nonce);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): Transaction {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseTransaction();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.messageType = reader.string();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.msg = Any.decode(reader, reader.uint32());
continue; }
case 3: {
if (tag !== 26) {
        break;
      }
    
        message.signature = Signature.decode(reader, reader.uint32());
continue; }
case 4: {
if (tag !== 32) {
        break;
      }
    
        message.createdHeight = reader.uint64() as bigint;
continue; }
case 5: {
if (tag !== 40) {
        break;
      }
    
        message.time = reader.uint64() as bigint;
continue; }
case 6: {
if (tag !== 48) {
        break;
      }
    
        message.fee = reader.uint64() as bigint;
continue; }
case 7: {
if (tag !== 58) {
        break;
      }
    
        message.memo = reader.string();
continue; }
case 8: {
if (tag !== 64) {
        break;
      }
    
        message.networkId = reader.uint64() as bigint;
continue; }
case 9: {
if (tag !== 72) {
        break;
      }
    
        message.chainId = reader.uint64() as bigint;
continue; }
case 10: {
if (tag !== 80) {
        break;
      }
    
        message.nonce = reader.uint64() as bigint;
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): Transaction {
      return {
messageType: isSet(object.messageType) ? globalThis.String(object.messageType)
         : isSet(object.message_type) ? globalThis.String(object.message_type)
          : "",
msg: isSet(object.msg) ? Any.fromJSON(object.msg)
        
          : undefined,
signature: isSet(object.signature) ? Signature.fromJSON(object.signature)
        
          : undefined,
createdHeight: isSet(object.createdHeight) ? BigInt(object.createdHeight)
         : isSet(object.created_height) ? BigInt(object.created_height)
          : 0n,
time: isSet(object.time) ? BigInt(object.time)
        
          : 0n,
fee: isSet(object.fee) ? BigInt(object.fee)
        
          : 0n,
memo: isSet(object.memo) ? globalThis.String(object.memo)
        
          : "",
networkId: isSet(object.networkId) ? BigInt(object.networkId)
         : isSet(object.network_id) ? BigInt(object.network_id)
          : 0n,
chainId: isSet(object.chainId) ? BigInt(object.chainId)
         : isSet(object.chain_id) ? BigInt(object.chain_id)
          : 0n,
nonce: isSet(object.nonce) ? BigInt(object.nonce)
        
          : 0n,
};
},

toJSON(message: Transaction): unknown {
      const obj: any = {};
if ( message.messageType !== "") {
          obj.messageType = message.messageType;
        }
if (message.msg !== undefined ) {
          obj.msg = Any.toJSON(message.msg);
        }
if (message.signature !== undefined ) {
          obj.signature = Signature.toJSON(message.signature);
        }
if ( message.createdHeight !== 0n) {
          obj.createdHeight = message.createdHeight.toString();
        }
if ( message.time !== 0n) {
          obj.time = message.time.toString();
        }
if ( message.fee !== 0n) {
          obj.fee = message.fee.toString();
        }
if ( message.memo !== "") {
          obj.memo = message.memo;
        }
if ( message.networkId !== 0n) {
          obj.networkId = message.networkId.toString();
        }
if ( message.chainId !== 0n) {
          obj.chainId = message.chainId.toString();
        }
if ( message.nonce !== 0n) {
          obj.nonce = message.nonce.toString();
        }
return obj;
},

create<I extends Exact<DeepPartial<Transaction>, I>>(base?: I): Transaction {
        return Transaction.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<Transaction>, I>>(object: I): Transaction {
const message = createBaseTransaction();
message.messageType = object.messageType ?? "";
message.msg = (object.msg !== undefined && object.msg !== null)
          ? Any.fromPartial(object.msg)
          : undefined;
message.signature = (object.signature !== undefined && object.signature !== null)
          ? Signature.fromPartial(object.signature)
          : undefined;
message.createdHeight = (object.createdHeight !== undefined && object.createdHeight !== null)
          ? BigInt(object.createdHeight)
          : 0n;
message.time = (object.time !== undefined && object.time !== null)
          ? BigInt(object.time)
          : 0n;
message.fee = (object.fee !== undefined && object.fee !== null)
          ? BigInt(object.fee)
          : 0n;
message.memo = object.memo ?? "";
message.networkId = (object.networkId !== undefined && object.networkId !== null)
          ? BigInt(object.networkId)
          : 0n;
message.chainId = (object.chainId !== undefined && object.chainId !== null)
          ? BigInt(object.chainId)
          : 0n;
message.nonce = (object.nonce !== undefined && object.nonce !== null)
          ? BigInt(object.nonce)
          : 0n;
return message;
}
            };

function createBaseMessageSend(): MessageSend {
      return { fromAddress: new Uint8Array(0),toAddress: new Uint8Array(0),amount: 0n };
    }

export const MessageSend: MessageFns<MessageSend> = {
              encode(
      message: MessageSend,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.fromAddress.length !== 0) {
          writer.uint32(10).bytes(message.fromAddress);
        }
if ( message.toAddress.length !== 0) {
          writer.uint32(18).bytes(message.toAddress);
        }
if ( message.amount !== 0n) {
          if (BigInt.asUintN(64, message.amount) !== message.amount) {
          throw new globalThis.Error('value provided for field message.amount of type uint64 too large');
        }
        writer.uint32(24).uint64(message.amount);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): MessageSend {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseMessageSend();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.fromAddress = reader.bytes();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.toAddress = reader.bytes();
continue; }
case 3: {
if (tag !== 24) {
        break;
      }
    
        message.amount = reader.uint64() as bigint;
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): MessageSend {
      return {
fromAddress: isSet(object.fromAddress) ? bytesFromBase64(object.fromAddress)
         : isSet(object.from_address) ? bytesFromBase64(object.from_address)
          : new Uint8Array(0),
toAddress: isSet(object.toAddress) ? bytesFromBase64(object.toAddress)
         : isSet(object.to_address) ? bytesFromBase64(object.to_address)
          : new Uint8Array(0),
amount: isSet(object.amount) ? BigInt(object.amount)
        
          : 0n,
};
},

toJSON(message: MessageSend): unknown {
      const obj: any = {};
if ( message.fromAddress.length !== 0) {
          obj.fromAddress = base64FromBytes(message.fromAddress);
        }
if ( message.toAddress.length !== 0) {
          obj.toAddress = base64FromBytes(message.toAddress);
        }
if ( message.amount !== 0n) {
          obj.amount = message.amount.toString();
        }
return obj;
},

create<I extends Exact<DeepPartial<MessageSend>, I>>(base?: I): MessageSend {
        return MessageSend.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<MessageSend>, I>>(object: I): MessageSend {
const message = createBaseMessageSend();
message.fromAddress = object.fromAddress ?? new Uint8Array(0);
message.toAddress = object.toAddress ?? new Uint8Array(0);
message.amount = (object.amount !== undefined && object.amount !== null)
          ? BigInt(object.amount)
          : 0n;
return message;
}
            };

function createBaseFeeParams(): FeeParams {
      return { sendFee: 0n };
    }

export const FeeParams: MessageFns<FeeParams> = {
              encode(
      message: FeeParams,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.sendFee !== 0n) {
          if (BigInt.asUintN(64, message.sendFee) !== message.sendFee) {
          throw new globalThis.Error('value provided for field message.sendFee of type uint64 too large');
        }
        writer.uint32(8).uint64(message.sendFee);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): FeeParams {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseFeeParams();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 8) {
        break;
      }
    
        message.sendFee = reader.uint64() as bigint;
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): FeeParams {
      return {
sendFee: isSet(object.sendFee) ? BigInt(object.sendFee)
         : isSet(object.send_fee) ? BigInt(object.send_fee)
          : 0n,
};
},

toJSON(message: FeeParams): unknown {
      const obj: any = {};
if ( message.sendFee !== 0n) {
          obj.sendFee = message.sendFee.toString();
        }
return obj;
},

create<I extends Exact<DeepPartial<FeeParams>, I>>(base?: I): FeeParams {
        return FeeParams.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<FeeParams>, I>>(object: I): FeeParams {
const message = createBaseFeeParams();
message.sendFee = (object.sendFee !== undefined && object.sendFee !== null)
          ? BigInt(object.sendFee)
          : 0n;
return message;
}
            };

function createBaseSignature(): Signature {
      return { publicKey: new Uint8Array(0),signature: new Uint8Array(0) };
    }

export const Signature: MessageFns<Signature> = {
              encode(
      message: Signature,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.publicKey.length !== 0) {
          writer.uint32(10).bytes(message.publicKey);
        }
if ( message.signature.length !== 0) {
          writer.uint32(18).bytes(message.signature);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): Signature {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseSignature();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.publicKey = reader.bytes();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.signature = reader.bytes();
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): Signature {
      return {
publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey)
         : isSet(object.public_key) ? bytesFromBase64(object.public_key)
          : new Uint8Array(0),
signature: isSet(object.signature) ? bytesFromBase64(object.signature)
        
          : new Uint8Array(0),
};
},

toJSON(message: Signature): unknown {
      const obj: any = {};
if ( message.publicKey.length !== 0) {
          obj.publicKey = base64FromBytes(message.publicKey);
        }
if ( message.signature.length !== 0) {
          obj.signature = base64FromBytes(message.signature);
        }
return obj;
},

create<I extends Exact<DeepPartial<Signature>, I>>(base?: I): Signature {
        return Signature.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<Signature>, I>>(object: I): Signature {
const message = createBaseSignature();
message.publicKey = object.publicKey ?? new Uint8Array(0);
message.signature = object.signature ?? new Uint8Array(0);
return message;
}
            };



function bytesFromBase64(b64: string): Uint8Array {
        
      const bin = globalThis.atob(b64);
      const arr = new Uint8Array(bin.length);
      for (let i = 0; i < bin.length; ++i) {
          arr[i] = bin.charCodeAt(i);
      }
      return arr;
    
      }

function base64FromBytes(arr: Uint8Array): string {
        
      const bin: string[] = [];
      arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
      });
      return globalThis.btoa(bin.join(''));
    
      }

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint :  T extends Builtin
        ? T
        
        : T extends globalThis.Array<infer U>
        ? globalThis.Array<DeepPartial<U>>
        : T extends ReadonlyArray<infer U>
        ? ReadonlyArray<DeepPartial<U>>
        : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
      export type Exact<P, I extends P> = P extends Builtin
        ? P
        : P &
        { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> >]: never };





















function isSet(value: any): boolean {
      return value !== null && value !== undefined;
    }









export interface MessageFns<T> {
        encode(message: T, writer?: BinaryWriter): BinaryWriter;
decode(input: BinaryReader | Uint8Array, length?: number): T;
fromJSON(object: any): T;
toJSON(message: T): unknown;
create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
      }