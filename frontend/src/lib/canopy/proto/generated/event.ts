// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: event.proto

    /* eslint-disable */
import { Any } from './google/protobuf/any';
import { BinaryWriter, BinaryReader } from '@bufbuild/protobuf/wire';

export const protobufPackage = 'types';

export interface Event {
/** event_type: The type of the transaction like 'slash' or 'unstake' */
eventType: string,
custom?: EventCustom | undefined,
/** height: the block height of the event */
height: bigint,
/** reference: the 'begin_block' / transaction hash / 'end_block' */
reference: string,
/** chainId: the chain id associated with the event */
chainId: bigint,
/**
 * block_height: the number of blocks in the blockchain before a specific block, indicating its position in the chain
 * and representing the blockchain's length
 */
blockHeight: bigint,
/**
 * block_hash: a unique identifier for a block, generated by applying a cryptographic hash function to the block's contents,
 * ensuring its integrity and connecting it to the previous block in the chain.
 */
blockHash: Uint8Array,
/** address: The address of the user affected by the event */
address: Uint8Array,
}

/** EventCustom carries a plugin-defined event payload. */
export interface EventCustom {
/** msg: custom payload. */
msg: Any | undefined,
}

function createBaseEvent(): Event {
      return { eventType: "",custom: undefined,height: 0n,reference: "",chainId: 0n,blockHeight: 0n,blockHash: new Uint8Array(0),address: new Uint8Array(0) };
    }

export const Event: MessageFns<Event> = {
              encode(
      message: Event,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.eventType !== "") {
          writer.uint32(10).string(message.eventType);
        }
if (message.custom !== undefined ) {
          EventCustom.encode(message.custom, writer.uint32(90).fork()).join();
        }
if ( message.height !== 0n) {
          if (BigInt.asUintN(64, message.height) !== message.height) {
          throw new globalThis.Error('value provided for field message.height of type uint64 too large');
        }
        writer.uint32(728).uint64(message.height);
        }
if ( message.reference !== "") {
          writer.uint32(738).string(message.reference);
        }
if ( message.chainId !== 0n) {
          if (BigInt.asUintN(64, message.chainId) !== message.chainId) {
          throw new globalThis.Error('value provided for field message.chainId of type uint64 too large');
        }
        writer.uint32(744).uint64(message.chainId);
        }
if ( message.blockHeight !== 0n) {
          if (BigInt.asUintN(64, message.blockHeight) !== message.blockHeight) {
          throw new globalThis.Error('value provided for field message.blockHeight of type uint64 too large');
        }
        writer.uint32(752).uint64(message.blockHeight);
        }
if ( message.blockHash.length !== 0) {
          writer.uint32(762).bytes(message.blockHash);
        }
if ( message.address.length !== 0) {
          writer.uint32(770).bytes(message.address);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): Event {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseEvent();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.eventType = reader.string();
continue; }
case 11: {
if (tag !== 90) {
        break;
      }
    
        message.custom = EventCustom.decode(reader, reader.uint32());
continue; }
case 91: {
if (tag !== 728) {
        break;
      }
    
        message.height = reader.uint64() as bigint;
continue; }
case 92: {
if (tag !== 738) {
        break;
      }
    
        message.reference = reader.string();
continue; }
case 93: {
if (tag !== 744) {
        break;
      }
    
        message.chainId = reader.uint64() as bigint;
continue; }
case 94: {
if (tag !== 752) {
        break;
      }
    
        message.blockHeight = reader.uint64() as bigint;
continue; }
case 95: {
if (tag !== 762) {
        break;
      }
    
        message.blockHash = reader.bytes();
continue; }
case 96: {
if (tag !== 770) {
        break;
      }
    
        message.address = reader.bytes();
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): Event {
      return {
eventType: isSet(object.eventType) ? globalThis.String(object.eventType)
         : isSet(object.event_type) ? globalThis.String(object.event_type)
          : "",
custom: isSet(object.custom) ? EventCustom.fromJSON(object.custom)
        
          : undefined,
height: isSet(object.height) ? BigInt(object.height)
        
          : 0n,
reference: isSet(object.reference) ? globalThis.String(object.reference)
        
          : "",
chainId: isSet(object.chainId) ? BigInt(object.chainId)
        
          : 0n,
blockHeight: isSet(object.blockHeight) ? BigInt(object.blockHeight)
         : isSet(object.block_height) ? BigInt(object.block_height)
          : 0n,
blockHash: isSet(object.blockHash) ? bytesFromBase64(object.blockHash)
         : isSet(object.block_hash) ? bytesFromBase64(object.block_hash)
          : new Uint8Array(0),
address: isSet(object.address) ? bytesFromBase64(object.address)
        
          : new Uint8Array(0),
};
},

toJSON(message: Event): unknown {
      const obj: any = {};
if ( message.eventType !== "") {
          obj.eventType = message.eventType;
        }
if (message.custom !== undefined ) {
          obj.custom = EventCustom.toJSON(message.custom);
        }
if ( message.height !== 0n) {
          obj.height = message.height.toString();
        }
if ( message.reference !== "") {
          obj.reference = message.reference;
        }
if ( message.chainId !== 0n) {
          obj.chainId = message.chainId.toString();
        }
if ( message.blockHeight !== 0n) {
          obj.blockHeight = message.blockHeight.toString();
        }
if ( message.blockHash.length !== 0) {
          obj.blockHash = base64FromBytes(message.blockHash);
        }
if ( message.address.length !== 0) {
          obj.address = base64FromBytes(message.address);
        }
return obj;
},

create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
        return Event.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
const message = createBaseEvent();
message.eventType = object.eventType ?? "";
message.custom = (object.custom !== undefined && object.custom !== null)
          ? EventCustom.fromPartial(object.custom)
          : undefined;
message.height = (object.height !== undefined && object.height !== null)
          ? BigInt(object.height)
          : 0n;
message.reference = object.reference ?? "";
message.chainId = (object.chainId !== undefined && object.chainId !== null)
          ? BigInt(object.chainId)
          : 0n;
message.blockHeight = (object.blockHeight !== undefined && object.blockHeight !== null)
          ? BigInt(object.blockHeight)
          : 0n;
message.blockHash = object.blockHash ?? new Uint8Array(0);
message.address = object.address ?? new Uint8Array(0);
return message;
}
            };

function createBaseEventCustom(): EventCustom {
      return { msg: undefined };
    }

export const EventCustom: MessageFns<EventCustom> = {
              encode(
      message: EventCustom,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if (message.msg !== undefined ) {
          Any.encode(message.msg, writer.uint32(10).fork()).join();
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): EventCustom {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseEventCustom();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.msg = Any.decode(reader, reader.uint32());
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): EventCustom {
      return {
msg: isSet(object.msg) ? Any.fromJSON(object.msg)
        
          : undefined,
};
},

toJSON(message: EventCustom): unknown {
      const obj: any = {};
if (message.msg !== undefined ) {
          obj.msg = Any.toJSON(message.msg);
        }
return obj;
},

create<I extends Exact<DeepPartial<EventCustom>, I>>(base?: I): EventCustom {
        return EventCustom.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<EventCustom>, I>>(object: I): EventCustom {
const message = createBaseEventCustom();
message.msg = (object.msg !== undefined && object.msg !== null)
          ? Any.fromPartial(object.msg)
          : undefined;
return message;
}
            };



function bytesFromBase64(b64: string): Uint8Array {
        
      const bin = globalThis.atob(b64);
      const arr = new Uint8Array(bin.length);
      for (let i = 0; i < bin.length; ++i) {
          arr[i] = bin.charCodeAt(i);
      }
      return arr;
    
      }

function base64FromBytes(arr: Uint8Array): string {
        
      const bin: string[] = [];
      arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
      });
      return globalThis.btoa(bin.join(''));
    
      }

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint :  T extends Builtin
        ? T
        
        : T extends globalThis.Array<infer U>
        ? globalThis.Array<DeepPartial<U>>
        : T extends ReadonlyArray<infer U>
        ? ReadonlyArray<DeepPartial<U>>
        : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
      export type Exact<P, I extends P> = P extends Builtin
        ? P
        : P &
        { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> >]: never };





















function isSet(value: any): boolean {
      return value !== null && value !== undefined;
    }









export interface MessageFns<T> {
        encode(message: T, writer?: BinaryWriter): BinaryWriter;
decode(input: BinaryReader | Uint8Array, length?: number): T;
fromJSON(object: any): T;
toJSON(message: T): unknown;
create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
      }