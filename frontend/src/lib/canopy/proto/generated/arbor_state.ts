// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: arbor_state.proto

    /* eslint-disable */
import { BinaryWriter, BinaryReader } from '@bufbuild/protobuf/wire';

export const protobufPackage = 'types';

/** MarketStatus mirrors ARCM's market.status enum (Section 9, Section 13, Section 16). */
export enum MarketStatus {
ACTIVE = 0,
/** PAUSED - governance action (ARCM Section 13) */
PAUSED = 1,
/** INSOLVENT - loss_factor reached zero (AYIS Section 5.4.3, I11) */
INSOLVENT = 2,
/** DEPRECATED - permanently retired (ARCM Section 19.2) */
DEPRECATED = 3,
}

export function marketStatusFromJSON(object: any): MarketStatus {
switch (object) {
case 0:
      case "ACTIVE":
        return MarketStatus.ACTIVE;
case 1:
      case "PAUSED":
        return MarketStatus.PAUSED;
case 2:
      case "INSOLVENT":
        return MarketStatus.INSOLVENT;
case 3:
      case "DEPRECATED":
        return MarketStatus.DEPRECATED;
default:
        throw new globalThis.Error("Unrecognized enum value " + object + " for enum MarketStatus");
}
}

export function marketStatusToJSON(object: MarketStatus): string {
switch (object) {
case MarketStatus.ACTIVE: return "ACTIVE";
case MarketStatus.PAUSED: return "PAUSED";
case MarketStatus.INSOLVENT: return "INSOLVENT";
case MarketStatus.DEPRECATED: return "DEPRECATED";
default:
        throw new globalThis.Error("Unrecognized enum value " + object + " for enum MarketStatus");
}
}

/**
 * Market is the {16} record: core market metadata plus the flags and counters
 * ARCM's bad-debt waterfall and freeze mechanism operate on. See ARCM v3.11.1
 * Section 19.1 for the canonical field set this implements.
 */
export interface Market {
marketId: string,
collateralAssetId: string,
debtAssetId: string,
/** 0-4, ARCM Section 4 LTV table */
assetTier: number,
/** AYIS Section 13 */
reserveFactorBps: bigint,
creator: Uint8Array,
status: MarketStatus,
/** ARCM Section 9.3a */
indexOverflowHalted: boolean,
/** ARCM Section 9.2b */
layer4PendingCount: number,
/** uint128, EncodeUint128 raw bytes (widened v3.11, L2-widen) */
layer4PendingBadDebtTotal: Uint8Array,
/** guarded via applyDebtDelta, C1 */
totalBorrowed: bigint,
/** ARCM Section 19.2.1b */
totalSupplied: bigint,
/** AYIS Section 7 Step 11 */
lastAccrualBlock: bigint,
/** ARCM Section 10 permissioned */
authorizedSubmitters: Uint8Array[],
}

/** BorrowerPosition is the {17} record (ARCM Section 6, AYIS Section 6). */
export interface BorrowerPosition {
marketId: string,
address: Uint8Array,
collateralQuantity: bigint,
debtPrincipal: bigint,
/** uint128, EncodeUint128 raw bytes -- B_index snapshot at open */
borrowIndexAtOpen: Uint8Array,
}

/**
 * PriceRecord is one {19} entry, keyed by (asset_id, submitter) --
 * ARCM Section 10's permissioned price feed. Multiple independent
 * submitters each write their own record per asset; quorum (MIN_REPORTERS)
 * and deviation (30% circuit breaker) are resolved by aggregating across
 * all of an asset's records at READ time, not by any single record being
 * authoritative on its own. block_height is chain-assigned at write time
 * (not submitter-supplied) for Section 11's staleness checks.
 */
export interface PriceRecord {
assetId: string,
submitter: Uint8Array,
/** USD per unit, x1e8 */
price: bigint,
confidenceBps: number,
blockHeight: bigint,
}

/** LenderPosition is the {24} record (AYIS Section 5.1). */
export interface LenderPosition {
marketId: string,
address: Uint8Array,
shares: bigint,
depositBlock: bigint,
}

/**
 * LossFactorQueueEntry is a {28} record (AYIS Section 12.4, ARCM v3.11.1
 * Section 9.3b Rule 3). One entry per market_id: a backstop-triggered
 * Layer 4 exhaustion enqueues a bad_debt amount here (via
 * EnqueueLossFactorApplication) rather than calling ApplyLossFactor()
 * synchronously, so BeginBlock's ProcessLossFactorQueue() can drain it
 * later in the same call under Section 12.3's Accrual Ordering Contract
 * (AccrueInterest MUST run before this queue drains). A market has at
 * most one outstanding entry at a time -- K3's idempotency guard in
 * ApplyLossFactor() means a second enqueue against an already-Insolvent
 * market is a no-op at drain time, not a second queued entry, so this
 * message does not need its own sequence/ordering field.
 */
export interface LossFactorQueueEntry {
marketId: string,
badDebt: bigint,
/** observability only; not read by any */
enqueuedBlock: bigint,
}

function createBaseMarket(): Market {
      return { marketId: "",collateralAssetId: "",debtAssetId: "",assetTier: 0,reserveFactorBps: 0n,creator: new Uint8Array(0),status: 0,indexOverflowHalted: false,layer4PendingCount: 0,layer4PendingBadDebtTotal: new Uint8Array(0),totalBorrowed: 0n,totalSupplied: 0n,lastAccrualBlock: 0n,authorizedSubmitters: [] };
    }

export const Market: MessageFns<Market> = {
              encode(
      message: Market,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.marketId !== "") {
          writer.uint32(10).string(message.marketId);
        }
if ( message.collateralAssetId !== "") {
          writer.uint32(18).string(message.collateralAssetId);
        }
if ( message.debtAssetId !== "") {
          writer.uint32(26).string(message.debtAssetId);
        }
if ( message.assetTier !== 0) {
          writer.uint32(32).uint32(message.assetTier);
        }
if ( message.reserveFactorBps !== 0n) {
          if (BigInt.asUintN(64, message.reserveFactorBps) !== message.reserveFactorBps) {
          throw new globalThis.Error('value provided for field message.reserveFactorBps of type uint64 too large');
        }
        writer.uint32(40).uint64(message.reserveFactorBps);
        }
if ( message.creator.length !== 0) {
          writer.uint32(50).bytes(message.creator);
        }
if ( message.status !== 0) {
          writer.uint32(56).int32(message.status);
        }
if ( message.indexOverflowHalted !== false) {
          writer.uint32(64).bool(message.indexOverflowHalted);
        }
if ( message.layer4PendingCount !== 0) {
          writer.uint32(72).uint32(message.layer4PendingCount);
        }
if ( message.layer4PendingBadDebtTotal.length !== 0) {
          writer.uint32(82).bytes(message.layer4PendingBadDebtTotal);
        }
if ( message.totalBorrowed !== 0n) {
          if (BigInt.asUintN(64, message.totalBorrowed) !== message.totalBorrowed) {
          throw new globalThis.Error('value provided for field message.totalBorrowed of type uint64 too large');
        }
        writer.uint32(88).uint64(message.totalBorrowed);
        }
if ( message.totalSupplied !== 0n) {
          if (BigInt.asUintN(64, message.totalSupplied) !== message.totalSupplied) {
          throw new globalThis.Error('value provided for field message.totalSupplied of type uint64 too large');
        }
        writer.uint32(96).uint64(message.totalSupplied);
        }
if ( message.lastAccrualBlock !== 0n) {
          if (BigInt.asUintN(64, message.lastAccrualBlock) !== message.lastAccrualBlock) {
          throw new globalThis.Error('value provided for field message.lastAccrualBlock of type uint64 too large');
        }
        writer.uint32(104).uint64(message.lastAccrualBlock);
        }
for (const v of message.authorizedSubmitters) {
            writer.uint32(114).bytes(v!);
          }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): Market {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseMarket();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.marketId = reader.string();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.collateralAssetId = reader.string();
continue; }
case 3: {
if (tag !== 26) {
        break;
      }
    
        message.debtAssetId = reader.string();
continue; }
case 4: {
if (tag !== 32) {
        break;
      }
    
        message.assetTier = reader.uint32();
continue; }
case 5: {
if (tag !== 40) {
        break;
      }
    
        message.reserveFactorBps = reader.uint64() as bigint;
continue; }
case 6: {
if (tag !== 50) {
        break;
      }
    
        message.creator = reader.bytes();
continue; }
case 7: {
if (tag !== 56) {
        break;
      }
    
        message.status = reader.int32() as any;
continue; }
case 8: {
if (tag !== 64) {
        break;
      }
    
        message.indexOverflowHalted = reader.bool();
continue; }
case 9: {
if (tag !== 72) {
        break;
      }
    
        message.layer4PendingCount = reader.uint32();
continue; }
case 10: {
if (tag !== 82) {
        break;
      }
    
        message.layer4PendingBadDebtTotal = reader.bytes();
continue; }
case 11: {
if (tag !== 88) {
        break;
      }
    
        message.totalBorrowed = reader.uint64() as bigint;
continue; }
case 12: {
if (tag !== 96) {
        break;
      }
    
        message.totalSupplied = reader.uint64() as bigint;
continue; }
case 13: {
if (tag !== 104) {
        break;
      }
    
        message.lastAccrualBlock = reader.uint64() as bigint;
continue; }
case 14: {
if (tag !== 114) {
        break;
      }
    
              
              message.authorizedSubmitters.push(reader.bytes());
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): Market {
      return {
marketId: isSet(object.marketId) ? globalThis.String(object.marketId)
         : isSet(object.market_id) ? globalThis.String(object.market_id)
          : "",
collateralAssetId: isSet(object.collateralAssetId) ? globalThis.String(object.collateralAssetId)
         : isSet(object.collateral_asset_id) ? globalThis.String(object.collateral_asset_id)
          : "",
debtAssetId: isSet(object.debtAssetId) ? globalThis.String(object.debtAssetId)
         : isSet(object.debt_asset_id) ? globalThis.String(object.debt_asset_id)
          : "",
assetTier: isSet(object.assetTier) ? globalThis.Number(object.assetTier)
         : isSet(object.asset_tier) ? globalThis.Number(object.asset_tier)
          : 0,
reserveFactorBps: isSet(object.reserveFactorBps) ? BigInt(object.reserveFactorBps)
         : isSet(object.reserve_factor_bps) ? BigInt(object.reserve_factor_bps)
          : 0n,
creator: isSet(object.creator) ? bytesFromBase64(object.creator)
        
          : new Uint8Array(0),
status: isSet(object.status) ? marketStatusFromJSON(object.status)
        
          : 0,
indexOverflowHalted: isSet(object.indexOverflowHalted) ? globalThis.Boolean(object.indexOverflowHalted)
         : isSet(object.index_overflow_halted) ? globalThis.Boolean(object.index_overflow_halted)
          : false,
layer4PendingCount: isSet(object.layer4PendingCount) ? globalThis.Number(object.layer4PendingCount)
         : isSet(object.layer4_pending_count) ? globalThis.Number(object.layer4_pending_count)
          : 0,
layer4PendingBadDebtTotal: isSet(object.layer4PendingBadDebtTotal) ? bytesFromBase64(object.layer4PendingBadDebtTotal)
         : isSet(object.layer4_pending_bad_debt_total) ? bytesFromBase64(object.layer4_pending_bad_debt_total)
          : new Uint8Array(0),
totalBorrowed: isSet(object.totalBorrowed) ? BigInt(object.totalBorrowed)
         : isSet(object.total_borrowed) ? BigInt(object.total_borrowed)
          : 0n,
totalSupplied: isSet(object.totalSupplied) ? BigInt(object.totalSupplied)
         : isSet(object.total_supplied) ? BigInt(object.total_supplied)
          : 0n,
lastAccrualBlock: isSet(object.lastAccrualBlock) ? BigInt(object.lastAccrualBlock)
         : isSet(object.last_accrual_block) ? BigInt(object.last_accrual_block)
          : 0n,
authorizedSubmitters: globalThis.Array.isArray(object?.authorizedSubmitters) ? object.authorizedSubmitters.map((e: any) => bytesFromBase64(e))  : globalThis.Array.isArray(object?.authorized_submitters) ? object.authorized_submitters.map((e: any) => bytesFromBase64(e)) : [],
};
},

toJSON(message: Market): unknown {
      const obj: any = {};
if ( message.marketId !== "") {
          obj.marketId = message.marketId;
        }
if ( message.collateralAssetId !== "") {
          obj.collateralAssetId = message.collateralAssetId;
        }
if ( message.debtAssetId !== "") {
          obj.debtAssetId = message.debtAssetId;
        }
if ( message.assetTier !== 0) {
          obj.assetTier = Math.round(message.assetTier);
        }
if ( message.reserveFactorBps !== 0n) {
          obj.reserveFactorBps = message.reserveFactorBps.toString();
        }
if ( message.creator.length !== 0) {
          obj.creator = base64FromBytes(message.creator);
        }
if ( message.status !== 0) {
          obj.status = marketStatusToJSON(message.status);
        }
if ( message.indexOverflowHalted !== false) {
          obj.indexOverflowHalted = message.indexOverflowHalted;
        }
if ( message.layer4PendingCount !== 0) {
          obj.layer4PendingCount = Math.round(message.layer4PendingCount);
        }
if ( message.layer4PendingBadDebtTotal.length !== 0) {
          obj.layer4PendingBadDebtTotal = base64FromBytes(message.layer4PendingBadDebtTotal);
        }
if ( message.totalBorrowed !== 0n) {
          obj.totalBorrowed = message.totalBorrowed.toString();
        }
if ( message.totalSupplied !== 0n) {
          obj.totalSupplied = message.totalSupplied.toString();
        }
if ( message.lastAccrualBlock !== 0n) {
          obj.lastAccrualBlock = message.lastAccrualBlock.toString();
        }
if (message.authorizedSubmitters?.length) {
          obj.authorizedSubmitters = message.authorizedSubmitters.map(e => base64FromBytes(e));
        }
return obj;
},

create<I extends Exact<DeepPartial<Market>, I>>(base?: I): Market {
        return Market.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<Market>, I>>(object: I): Market {
const message = createBaseMarket();
message.marketId = object.marketId ?? "";
message.collateralAssetId = object.collateralAssetId ?? "";
message.debtAssetId = object.debtAssetId ?? "";
message.assetTier = object.assetTier ?? 0;
message.reserveFactorBps = (object.reserveFactorBps !== undefined && object.reserveFactorBps !== null)
          ? BigInt(object.reserveFactorBps)
          : 0n;
message.creator = object.creator ?? new Uint8Array(0);
message.status = object.status ?? 0;
message.indexOverflowHalted = object.indexOverflowHalted ?? false;
message.layer4PendingCount = object.layer4PendingCount ?? 0;
message.layer4PendingBadDebtTotal = object.layer4PendingBadDebtTotal ?? new Uint8Array(0);
message.totalBorrowed = (object.totalBorrowed !== undefined && object.totalBorrowed !== null)
          ? BigInt(object.totalBorrowed)
          : 0n;
message.totalSupplied = (object.totalSupplied !== undefined && object.totalSupplied !== null)
          ? BigInt(object.totalSupplied)
          : 0n;
message.lastAccrualBlock = (object.lastAccrualBlock !== undefined && object.lastAccrualBlock !== null)
          ? BigInt(object.lastAccrualBlock)
          : 0n;
message.authorizedSubmitters = object.authorizedSubmitters?.map((e) => e) || [];
return message;
}
            };

function createBaseBorrowerPosition(): BorrowerPosition {
      return { marketId: "",address: new Uint8Array(0),collateralQuantity: 0n,debtPrincipal: 0n,borrowIndexAtOpen: new Uint8Array(0) };
    }

export const BorrowerPosition: MessageFns<BorrowerPosition> = {
              encode(
      message: BorrowerPosition,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.marketId !== "") {
          writer.uint32(10).string(message.marketId);
        }
if ( message.address.length !== 0) {
          writer.uint32(18).bytes(message.address);
        }
if ( message.collateralQuantity !== 0n) {
          if (BigInt.asUintN(64, message.collateralQuantity) !== message.collateralQuantity) {
          throw new globalThis.Error('value provided for field message.collateralQuantity of type uint64 too large');
        }
        writer.uint32(24).uint64(message.collateralQuantity);
        }
if ( message.debtPrincipal !== 0n) {
          if (BigInt.asUintN(64, message.debtPrincipal) !== message.debtPrincipal) {
          throw new globalThis.Error('value provided for field message.debtPrincipal of type uint64 too large');
        }
        writer.uint32(32).uint64(message.debtPrincipal);
        }
if ( message.borrowIndexAtOpen.length !== 0) {
          writer.uint32(42).bytes(message.borrowIndexAtOpen);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): BorrowerPosition {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseBorrowerPosition();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.marketId = reader.string();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.address = reader.bytes();
continue; }
case 3: {
if (tag !== 24) {
        break;
      }
    
        message.collateralQuantity = reader.uint64() as bigint;
continue; }
case 4: {
if (tag !== 32) {
        break;
      }
    
        message.debtPrincipal = reader.uint64() as bigint;
continue; }
case 5: {
if (tag !== 42) {
        break;
      }
    
        message.borrowIndexAtOpen = reader.bytes();
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): BorrowerPosition {
      return {
marketId: isSet(object.marketId) ? globalThis.String(object.marketId)
         : isSet(object.market_id) ? globalThis.String(object.market_id)
          : "",
address: isSet(object.address) ? bytesFromBase64(object.address)
        
          : new Uint8Array(0),
collateralQuantity: isSet(object.collateralQuantity) ? BigInt(object.collateralQuantity)
         : isSet(object.collateral_quantity) ? BigInt(object.collateral_quantity)
          : 0n,
debtPrincipal: isSet(object.debtPrincipal) ? BigInt(object.debtPrincipal)
         : isSet(object.debt_principal) ? BigInt(object.debt_principal)
          : 0n,
borrowIndexAtOpen: isSet(object.borrowIndexAtOpen) ? bytesFromBase64(object.borrowIndexAtOpen)
         : isSet(object.borrow_index_at_open) ? bytesFromBase64(object.borrow_index_at_open)
          : new Uint8Array(0),
};
},

toJSON(message: BorrowerPosition): unknown {
      const obj: any = {};
if ( message.marketId !== "") {
          obj.marketId = message.marketId;
        }
if ( message.address.length !== 0) {
          obj.address = base64FromBytes(message.address);
        }
if ( message.collateralQuantity !== 0n) {
          obj.collateralQuantity = message.collateralQuantity.toString();
        }
if ( message.debtPrincipal !== 0n) {
          obj.debtPrincipal = message.debtPrincipal.toString();
        }
if ( message.borrowIndexAtOpen.length !== 0) {
          obj.borrowIndexAtOpen = base64FromBytes(message.borrowIndexAtOpen);
        }
return obj;
},

create<I extends Exact<DeepPartial<BorrowerPosition>, I>>(base?: I): BorrowerPosition {
        return BorrowerPosition.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<BorrowerPosition>, I>>(object: I): BorrowerPosition {
const message = createBaseBorrowerPosition();
message.marketId = object.marketId ?? "";
message.address = object.address ?? new Uint8Array(0);
message.collateralQuantity = (object.collateralQuantity !== undefined && object.collateralQuantity !== null)
          ? BigInt(object.collateralQuantity)
          : 0n;
message.debtPrincipal = (object.debtPrincipal !== undefined && object.debtPrincipal !== null)
          ? BigInt(object.debtPrincipal)
          : 0n;
message.borrowIndexAtOpen = object.borrowIndexAtOpen ?? new Uint8Array(0);
return message;
}
            };

function createBasePriceRecord(): PriceRecord {
      return { assetId: "",submitter: new Uint8Array(0),price: 0n,confidenceBps: 0,blockHeight: 0n };
    }

export const PriceRecord: MessageFns<PriceRecord> = {
              encode(
      message: PriceRecord,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.assetId !== "") {
          writer.uint32(10).string(message.assetId);
        }
if ( message.submitter.length !== 0) {
          writer.uint32(18).bytes(message.submitter);
        }
if ( message.price !== 0n) {
          if (BigInt.asUintN(64, message.price) !== message.price) {
          throw new globalThis.Error('value provided for field message.price of type uint64 too large');
        }
        writer.uint32(24).uint64(message.price);
        }
if ( message.confidenceBps !== 0) {
          writer.uint32(32).uint32(message.confidenceBps);
        }
if ( message.blockHeight !== 0n) {
          if (BigInt.asUintN(64, message.blockHeight) !== message.blockHeight) {
          throw new globalThis.Error('value provided for field message.blockHeight of type uint64 too large');
        }
        writer.uint32(40).uint64(message.blockHeight);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): PriceRecord {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBasePriceRecord();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.assetId = reader.string();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.submitter = reader.bytes();
continue; }
case 3: {
if (tag !== 24) {
        break;
      }
    
        message.price = reader.uint64() as bigint;
continue; }
case 4: {
if (tag !== 32) {
        break;
      }
    
        message.confidenceBps = reader.uint32();
continue; }
case 5: {
if (tag !== 40) {
        break;
      }
    
        message.blockHeight = reader.uint64() as bigint;
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): PriceRecord {
      return {
assetId: isSet(object.assetId) ? globalThis.String(object.assetId)
         : isSet(object.asset_id) ? globalThis.String(object.asset_id)
          : "",
submitter: isSet(object.submitter) ? bytesFromBase64(object.submitter)
        
          : new Uint8Array(0),
price: isSet(object.price) ? BigInt(object.price)
        
          : 0n,
confidenceBps: isSet(object.confidenceBps) ? globalThis.Number(object.confidenceBps)
         : isSet(object.confidence_bps) ? globalThis.Number(object.confidence_bps)
          : 0,
blockHeight: isSet(object.blockHeight) ? BigInt(object.blockHeight)
         : isSet(object.block_height) ? BigInt(object.block_height)
          : 0n,
};
},

toJSON(message: PriceRecord): unknown {
      const obj: any = {};
if ( message.assetId !== "") {
          obj.assetId = message.assetId;
        }
if ( message.submitter.length !== 0) {
          obj.submitter = base64FromBytes(message.submitter);
        }
if ( message.price !== 0n) {
          obj.price = message.price.toString();
        }
if ( message.confidenceBps !== 0) {
          obj.confidenceBps = Math.round(message.confidenceBps);
        }
if ( message.blockHeight !== 0n) {
          obj.blockHeight = message.blockHeight.toString();
        }
return obj;
},

create<I extends Exact<DeepPartial<PriceRecord>, I>>(base?: I): PriceRecord {
        return PriceRecord.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<PriceRecord>, I>>(object: I): PriceRecord {
const message = createBasePriceRecord();
message.assetId = object.assetId ?? "";
message.submitter = object.submitter ?? new Uint8Array(0);
message.price = (object.price !== undefined && object.price !== null)
          ? BigInt(object.price)
          : 0n;
message.confidenceBps = object.confidenceBps ?? 0;
message.blockHeight = (object.blockHeight !== undefined && object.blockHeight !== null)
          ? BigInt(object.blockHeight)
          : 0n;
return message;
}
            };

function createBaseLenderPosition(): LenderPosition {
      return { marketId: "",address: new Uint8Array(0),shares: 0n,depositBlock: 0n };
    }

export const LenderPosition: MessageFns<LenderPosition> = {
              encode(
      message: LenderPosition,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.marketId !== "") {
          writer.uint32(10).string(message.marketId);
        }
if ( message.address.length !== 0) {
          writer.uint32(18).bytes(message.address);
        }
if ( message.shares !== 0n) {
          if (BigInt.asUintN(64, message.shares) !== message.shares) {
          throw new globalThis.Error('value provided for field message.shares of type uint64 too large');
        }
        writer.uint32(24).uint64(message.shares);
        }
if ( message.depositBlock !== 0n) {
          if (BigInt.asUintN(64, message.depositBlock) !== message.depositBlock) {
          throw new globalThis.Error('value provided for field message.depositBlock of type uint64 too large');
        }
        writer.uint32(32).uint64(message.depositBlock);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): LenderPosition {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseLenderPosition();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.marketId = reader.string();
continue; }
case 2: {
if (tag !== 18) {
        break;
      }
    
        message.address = reader.bytes();
continue; }
case 3: {
if (tag !== 24) {
        break;
      }
    
        message.shares = reader.uint64() as bigint;
continue; }
case 4: {
if (tag !== 32) {
        break;
      }
    
        message.depositBlock = reader.uint64() as bigint;
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): LenderPosition {
      return {
marketId: isSet(object.marketId) ? globalThis.String(object.marketId)
         : isSet(object.market_id) ? globalThis.String(object.market_id)
          : "",
address: isSet(object.address) ? bytesFromBase64(object.address)
        
          : new Uint8Array(0),
shares: isSet(object.shares) ? BigInt(object.shares)
        
          : 0n,
depositBlock: isSet(object.depositBlock) ? BigInt(object.depositBlock)
         : isSet(object.deposit_block) ? BigInt(object.deposit_block)
          : 0n,
};
},

toJSON(message: LenderPosition): unknown {
      const obj: any = {};
if ( message.marketId !== "") {
          obj.marketId = message.marketId;
        }
if ( message.address.length !== 0) {
          obj.address = base64FromBytes(message.address);
        }
if ( message.shares !== 0n) {
          obj.shares = message.shares.toString();
        }
if ( message.depositBlock !== 0n) {
          obj.depositBlock = message.depositBlock.toString();
        }
return obj;
},

create<I extends Exact<DeepPartial<LenderPosition>, I>>(base?: I): LenderPosition {
        return LenderPosition.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<LenderPosition>, I>>(object: I): LenderPosition {
const message = createBaseLenderPosition();
message.marketId = object.marketId ?? "";
message.address = object.address ?? new Uint8Array(0);
message.shares = (object.shares !== undefined && object.shares !== null)
          ? BigInt(object.shares)
          : 0n;
message.depositBlock = (object.depositBlock !== undefined && object.depositBlock !== null)
          ? BigInt(object.depositBlock)
          : 0n;
return message;
}
            };

function createBaseLossFactorQueueEntry(): LossFactorQueueEntry {
      return { marketId: "",badDebt: 0n,enqueuedBlock: 0n };
    }

export const LossFactorQueueEntry: MessageFns<LossFactorQueueEntry> = {
              encode(
      message: LossFactorQueueEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
if ( message.marketId !== "") {
          writer.uint32(10).string(message.marketId);
        }
if ( message.badDebt !== 0n) {
          if (BigInt.asUintN(64, message.badDebt) !== message.badDebt) {
          throw new globalThis.Error('value provided for field message.badDebt of type uint64 too large');
        }
        writer.uint32(16).uint64(message.badDebt);
        }
if ( message.enqueuedBlock !== 0n) {
          if (BigInt.asUintN(64, message.enqueuedBlock) !== message.enqueuedBlock) {
          throw new globalThis.Error('value provided for field message.enqueuedBlock of type uint64 too large');
        }
        writer.uint32(24).uint64(message.enqueuedBlock);
        }
return writer;
},

decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): LossFactorQueueEntry {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
const message = createBaseLossFactorQueueEntry();
while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
case 1: {
if (tag !== 10) {
        break;
      }
    
        message.marketId = reader.string();
continue; }
case 2: {
if (tag !== 16) {
        break;
      }
    
        message.badDebt = reader.uint64() as bigint;
continue; }
case 3: {
if (tag !== 24) {
        break;
      }
    
        message.enqueuedBlock = reader.uint64() as bigint;
continue; }
}
if ((tag & 7) === 4 || tag === 0) {
        break;
      }
reader.skip(tag & 7);
}
return message;
},

fromJSON(object: any): LossFactorQueueEntry {
      return {
marketId: isSet(object.marketId) ? globalThis.String(object.marketId)
         : isSet(object.market_id) ? globalThis.String(object.market_id)
          : "",
badDebt: isSet(object.badDebt) ? BigInt(object.badDebt)
         : isSet(object.bad_debt) ? BigInt(object.bad_debt)
          : 0n,
enqueuedBlock: isSet(object.enqueuedBlock) ? BigInt(object.enqueuedBlock)
         : isSet(object.enqueued_block) ? BigInt(object.enqueued_block)
          : 0n,
};
},

toJSON(message: LossFactorQueueEntry): unknown {
      const obj: any = {};
if ( message.marketId !== "") {
          obj.marketId = message.marketId;
        }
if ( message.badDebt !== 0n) {
          obj.badDebt = message.badDebt.toString();
        }
if ( message.enqueuedBlock !== 0n) {
          obj.enqueuedBlock = message.enqueuedBlock.toString();
        }
return obj;
},

create<I extends Exact<DeepPartial<LossFactorQueueEntry>, I>>(base?: I): LossFactorQueueEntry {
        return LossFactorQueueEntry.fromPartial(base ?? ({} as any));
      },
fromPartial<I extends Exact<DeepPartial<LossFactorQueueEntry>, I>>(object: I): LossFactorQueueEntry {
const message = createBaseLossFactorQueueEntry();
message.marketId = object.marketId ?? "";
message.badDebt = (object.badDebt !== undefined && object.badDebt !== null)
          ? BigInt(object.badDebt)
          : 0n;
message.enqueuedBlock = (object.enqueuedBlock !== undefined && object.enqueuedBlock !== null)
          ? BigInt(object.enqueuedBlock)
          : 0n;
return message;
}
            };



function bytesFromBase64(b64: string): Uint8Array {
        
      const bin = globalThis.atob(b64);
      const arr = new Uint8Array(bin.length);
      for (let i = 0; i < bin.length; ++i) {
          arr[i] = bin.charCodeAt(i);
      }
      return arr;
    
      }

function base64FromBytes(arr: Uint8Array): string {
        
      const bin: string[] = [];
      arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
      });
      return globalThis.btoa(bin.join(''));
    
      }

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint :  T extends Builtin
        ? T
        
        : T extends globalThis.Array<infer U>
        ? globalThis.Array<DeepPartial<U>>
        : T extends ReadonlyArray<infer U>
        ? ReadonlyArray<DeepPartial<U>>
        : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
      export type Exact<P, I extends P> = P extends Builtin
        ? P
        : P &
        { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> >]: never };





















function isSet(value: any): boolean {
      return value !== null && value !== undefined;
    }









export interface MessageFns<T> {
        encode(message: T, writer?: BinaryWriter): BinaryWriter;
decode(input: BinaryReader | Uint8Array, length?: number): T;
fromJSON(object: any): T;
toJSON(message: T): unknown;
create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
      }